/* eslint-disable @typescript-eslint/naming-convention */
import type { VFC } from "react";
import { Layout } from "src/components/layout";

const About: VFC = () => {
  return (
    <Layout>
      <p className="block py-10 w-full text-center">こちらのページは現在準備中になります。。。</p>
    </Layout>
  );
};

export default About;
